import LegalDocument from '../../../components/LegalDocument';

export const metadata = { title: 'Cookie Policy — VirtualTrade' };

export default function CookiesPage() {
  return <LegalDocument slug="cookies" />;
}
