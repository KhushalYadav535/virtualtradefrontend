import LegalDocument from '../../../components/LegalDocument';

export const metadata = { title: 'Licenses & Attributions — VirtualTrade' };

export default function LicensesPage() {
  return <LegalDocument slug="licenses" />;
}
